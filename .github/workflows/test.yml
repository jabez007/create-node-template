name: Run Shared Test Workflow

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - main
      - master

jobs:
  run_tests:
    uses: jabez007/create-node-template/.github/workflows/shared.test.yml@master
    secrets: inherit
